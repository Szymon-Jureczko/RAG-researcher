domains:
  ml:
    arxiv_query: "cs.LG OR cs.AI"
  physics:
    arxiv_query: "physics.gen-ph"
  bio:
    arxiv_query: "q-bio"

chunking:
  chunk_size: 1000
  chunk_overlap: 200

paths:
  pdf_dir: "data/papers/"
  faiss_index: "data/faiss_index"
