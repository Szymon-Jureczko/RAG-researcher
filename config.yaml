sources:
  arxiv:
    max_results: 100
  local:
    pdf_dir: "data/papers/"

chunking:
  chunk_size: 1000
  chunk_overlap: 200

embeddings:
  model: "sentence-transformers/all-MiniLM-L6-v2"

vector_store:
  faiss_index_path: "data/faiss_index"
