# ── Core ──
langchain>=0.3.0,<0.4.0
langchain-core>=0.3.0,<0.4.0
langchain-community>=0.3.0,<0.4.0
langchain-openai>=0.2.0,<0.3.0
langchain-huggingface>=0.1.0,<0.2.0
langchain-text-splitters>=0.3.0,<0.4.0

# ── Document parsing ──
pymupdf4llm>=0.0.10
arxiv>=2.1.0

# ── Vector stores & search ──
faiss-cpu>=1.7.4
rank-bm25>=0.2.2

# ── Weaviate (production hybrid search) ──
weaviate-client>=4.4.0

# ── Embeddings ──
sentence-transformers>=2.6.0

# ── Configuration ──
pyyaml>=6.0
python-dotenv>=1.0.0

# ── Frontend ──
streamlit>=1.32.0

# ── Additional ingestion sources ──
xmltodict>=0.13.0     # PubMed (PubMedLoader dependency)
wikipedia>=1.4.0      # Wikipedia articles

# ── Evaluation (RAG Triad / LLM-as-a-Judge) ──
ragas>=0.2.0
datasets>=2.14.0
pandas>=2.0.0
rapidfuzz>=3.0.0      # ragas testset generation (OverlapScoreBuilder dependency)
